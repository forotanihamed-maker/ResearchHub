# قواعد تغییر
فرایند: بررسی → گزارش → طراحی → تأیید → پیاده‌سازی → Typecheck/Build/Lint → تست رفتار → گزارش.
Schema/Migration/معماری بدون تأیید مناسب تغییر نکند.
Minimum Change رعایت شود.
اگر ساختار موجود نیاز را برطرف می‌کند، ساختار موازی نساز.
