# قواعد تست
حداقل: typecheck، build و در صورت وجود config، lint.
قابلیت‌های حساس: Login، Role، Authorization، Project access و API.
UI: Loading، Empty، Error، RTL و Responsive.
اگر تستی اجرا نشده، صریحاً «اجرا نشده» نوشته شود.
